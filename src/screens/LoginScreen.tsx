import {
    Button,
    Text,
    View
} from 'react-native';

export function LoginScreen() {
  return (
    <View>
      <Text>Log in</Text>
      <Button title="Log in" onPress={() => {}} />
    </View>
  );
}